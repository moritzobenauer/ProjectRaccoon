# test if all resolutions are in atomisitc, united atomes or cg?

# test it all atoms counts == sum of atoms in atoms list

# test if, keys occur multiple times in the monomer file -> if two monomers have the same name and resolution -> error
